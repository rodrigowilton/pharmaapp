import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pharmaapp.app',
  appName: 'PharmaApp',
  webDir: 'www',
  server: { androidScheme: 'https' }
};

export default config;
